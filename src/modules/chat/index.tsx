import React from "react";
import { Layout } from "../../common/components/layout";

interface IProps {}

export const Chat = (props: IProps) => {
  return (
    <Layout>
      <p>Chat page </p>
    </Layout>
  );
};
