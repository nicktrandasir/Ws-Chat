import React from "react";
import { Layout } from "../../common/components/layout";

interface IProps {}

export const History = (props: IProps) => {
  return (
    <Layout>
      <p>History page </p>
    </Layout>
  );
};
