import React from "react";
import { Layout } from "../../common/components/layout";

interface IProps {}

export const Cabinet = (props: IProps) => {
  return (
    <Layout>
      <p>Cabinet page </p>
    </Layout>
  );
};
