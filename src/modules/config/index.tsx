import React from "react";
import { Layout } from "../../common/components/layout";

interface IProps {}

export const Config = (props: IProps) => {
  return (
    <Layout>
      <p>Config page </p>
    </Layout>
  );
};
