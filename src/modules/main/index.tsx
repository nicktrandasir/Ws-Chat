import React from "react";
import { Layout } from "../../common/components/layout";

interface IProps {}

export const Main = (props: IProps) => {
  return (
    <Layout>
      <p>Main page </p>
    </Layout>
  );
};
